<%*
// ===== 新文章模板：自动创建 site/content/posts/<slug>/index.md =====
// 触发方式：
//   A. 自动：在 posts/ 下新建笔记（Templater 文件夹模板）→ 标题栏输入标题，或自动弹出询问
//   B. 手动：任意文件 Ctrl+P → Templater: 插入模板 → 选择「新文章」
let originalTitle = tp.file.title;
if (!originalTitle || originalTitle === "无标题" || originalTitle === "Untitled") {
  originalTitle = await tp.system.prompt("文章标题：");
}
const date = tp.date.now("YYYY-MM-DD");
const slug = originalTitle
  .replace(/[\\/:*?"<>|]+/g, "-")
  .replace(/\s+/g, "-")
  .replace(/-+/g, "-")
  .replace(/^-|-$/g, "");
await tp.file.move(`site/content/posts/${slug}/index`); // 不带 .md：Obsidian 自动补全（带 .md 会变 index.md.md）
tR += `---
title: ${originalTitle}
date: ${date}
lastmod: ${date}
categories:
  - 学习路径
tags: []
draft: false
description: ""
---

> 一句话说明这篇文章解决什么问题

## 背景

<!-- 为什么写这篇？上下文 / 动机 -->

## 正文

<!--
  - 高亮：<mark class="va-hl">重点</mark>（Obsidian 写作时直接写 mark 标签，网站渲染青蓝霓虹高亮）
  - 知识总结：核心概念 → 关键细节 → 示例 → 参考
  - 阅读小结：一句话总结 → 核心观点 → 行动清单 → 对学习路径的贡献
  - 学习路径：知识点清单 - [ ] 未学 / - [x] 已学
-->

## 总结

<!--
  站内链接：[文字](/posts/文章slug/)（绝对路径，需手动替换 slug）
  description 可留空：网站卡片自动从正文截取摘要
  分类可选值：学习路径 / 周记 / 精读 / 站点指南（首页星图按此分组）
-->
`;
%>