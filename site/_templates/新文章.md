<%*
// ===== 新文章模板：自动创建 site/content/posts/<slug>/index.md =====
// 用法：新建任意笔记 → Ctrl+P → Templater: 插入模板 → 选择「新文章」
const originalTitle = tp.file.title;
const date = tp.date.now("YYYY-MM-DD");
const slug = originalTitle
  .replace(/[\\/:*?"<>|]+/g, "-")
  .replace(/\s+/g, "-")
  .replace(/-+/g, "-")
  .replace(/^-|-$/g, "");
await tp.file.move(`site/content/posts/${slug}/index.md`);
tR += `---
title: ${originalTitle}
date: ${date}
lastmod: ${date}
categories:
  - 未分类
tags: []
draft: false
description: ""
---

> 一句话说明这篇文章解决什么问题

## 背景

<!-- 为什么写这篇？上下文 / 动机 -->

## 正文

<!--
  - 知识总结：核心概念 → 关键细节 → 示例 → 参考
  - 阅读小结：一句话总结 → 核心观点 → 行动清单 → 对学习路径的贡献
  - 学习路径：知识点清单 - [ ] 未学 / - [x] 已学
-->

## 总结

<!-- 站内链接：[[笔记名]] 或 [文字](/posts/文章slug/)（需手动替换 slug） -->
`;
%>